# ML stack for FinBERT on EC2 (CPU). Install PyTorch first, then this file:
#
#   backend/venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cpu
#   backend/venv/bin/pip install -r backend/requirements-ml.txt
#
transformers>=4.38.0,<5
sentencepiece>=0.1.99
safetensors>=0.4.0
accelerate>=0.27.0
