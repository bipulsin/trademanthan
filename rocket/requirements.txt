# Rocket backtester — prefer packages already used by TradeManthan backend.
# Install only what is missing; do not pin conflicting older numpy/pandas on Py3.12+.
sqlalchemy>=2.0
psycopg2-binary>=2.9
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
httpx>=0.25
requests>=2.32
pandas>=2.0
numpy>=1.24
pytz>=2024.1
typer>=0.12
rich>=13.0
plotly>=5.0
pyarrow>=14.0
# Optional: polars, duckdb, asyncpg, upstox-python-sdk — not required (Upstox via backend.services.upstox_service)
