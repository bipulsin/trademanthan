diff --git a/Dockerfile b/Dockerfile
index e367910..b97f286 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -43,6 +43,8 @@ FROM base AS build
 WORKDIR /app
 COPY --from=deps /app /app
 COPY . .
+ARG PAPERCLIP_UI_BASE_PATH=/
+ENV PAPERCLIP_UI_BASE_PATH=${PAPERCLIP_UI_BASE_PATH}
 RUN pnpm --filter @paperclipai/ui build
 RUN pnpm --filter @paperclipai/plugin-sdk build
 RUN pnpm --filter @paperclipai/server build
diff --git a/ui/src/main.tsx b/ui/src/main.tsx
index b37c5a0..7289dc0 100644
--- a/ui/src/main.tsx
+++ b/ui/src/main.tsx
@@ -22,9 +22,13 @@ import "./index.css";
 
 initPluginBridge(React, ReactDOM);
 
+const viteBase = import.meta.env.BASE_URL;
+const routerBasename = viteBase === "/" ? undefined : viteBase.replace(/\/$/, "");
+
 if ("serviceWorker" in navigator) {
   window.addEventListener("load", () => {
-    navigator.serviceWorker.register("/sw.js");
+    const swPath = new URL("sw.js", new URL(viteBase, window.location.origin).href).pathname;
+    navigator.serviceWorker.register(swPath);
   });
 }
 
@@ -46,7 +50,7 @@ createRoot(document.getElementById("root")!).render(
   <StrictMode>
     <QueryClientProvider client={queryClient}>
       <ThemeProvider>
-        <BrowserRouter>
+        <BrowserRouter basename={routerBasename}>
           <CompanyProvider>
             <EditorAutocompleteProvider>
               <ToastProvider>
diff --git a/ui/vite.config.ts b/ui/vite.config.ts
index 27f481a..44f8372 100644
--- a/ui/vite.config.ts
+++ b/ui/vite.config.ts
@@ -4,7 +4,15 @@ import react from "@vitejs/plugin-react";
 import tailwindcss from "@tailwindcss/vite";
 import { createUiDevWatchOptions } from "./src/lib/vite-watch";
 
+function vitePublicBase(): string {
+  const raw = process.env.PAPERCLIP_UI_BASE_PATH || process.env.BASE_PATH || "/";
+  const t = raw.trim() || "/";
+  if (t === "/") return "/";
+  return t.endsWith("/") ? t : `${t}/`;
+}
+
 export default defineConfig(({ mode }) => ({
+  base: vitePublicBase(),
   plugins: [react(), tailwindcss()],
   build: {
     minify: "esbuild",
